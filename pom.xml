<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.promql</groupId>
    <artifactId>promql-java</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>promql-java</name>
    <description>PromQL 解析/打印库：PromQL 字符串 ⇄ AST。移植自 Prometheus 官方 Go 实现（语法层）。</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- 语言下限 Java 17（Q2 决定）；工具链为 21 时以 release 17 编译 -->
        <maven.compiler.release>17</maven.compiler.release>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <junit.jupiter.version>5.10.2</junit.jupiter.version>
    </properties>

    <!-- 零运行时依赖（Q6 决定）；JUnit 5 仅测试作用域 -->
    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <!-- 移植自 parse_test.go 的压力用例（万层 "-{}-1" 链）需要深递归：
                         Go 协程栈可动态增长，JVM 线程栈固定，故调大（见 PORTING.md 分歧清单）。 -->
                    <argLine>-Xss32m</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
